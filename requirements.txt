# NOTE: sum_by_gpt.py, eval_by_gpt.py and cnndm_gpt_data_creator.py use the
# legacy openai<1.0 global-client API (`openai.api_key = ...`). Do not upgrade
# past 0.28 without porting those three scripts to the openai>=1.0 client.
datasets==2.14.5
evaluate==0.4.0
numpy==1.26.2
openai==0.28.0
openai_multi_client==0.1.1
pandas==2.1.4
torch==2.0.1
tqdm==4.66.1
transformers==4.36.0
